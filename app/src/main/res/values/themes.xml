<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema antigo (pode deixar se quiser) -->
    <style name="Theme.WeatherApp" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- NOVO TEMA ORBITA (Adicione isto) -->
    <style name="Theme.Orbita" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">#050B14</item> <!-- Cor do fundo espacial -->
        <item name="android:windowLightStatusBar">false</item> <!-- Ícones da barra brancos -->
    </style>
</resources>